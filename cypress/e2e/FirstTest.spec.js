// spec can contain few describes
describe('My first test suite', () => {

    // describe can contain describe
    describe('Suite section', () => {
        beforeEach('Code for every test inside this describe', () => {
            // code before every test
        })

        it('some test', () => {

        })
    })

// start new test/action with 'it()' word 
    it('first test', () => {

    })

    it('second test', () => {

    })

})

describe('My first test suite', () => {

        it('first test', () => {
    
        })
    
        it('second test', () => {
    
        })
    
    })
